# Python Version

Python version = 3.11.9

# Dependencies

```bash
cd SolarPanels
python -m venv .venv
# Windows
.\.venv\Scripts\Activate
# Linux/macOS
source .venv/bin/activate
pip install -r requirements.txt
```
---

Check README.md files inside each folder on how to run inverter scripts. 